.venv/
__pycache__/
*.pyc

# Pipeline video output — too big for GitHub's 100 MB file size limit
*.mp4

# KITTI grayscale image data — keep only sequence 00 (the one the script uses)
data/data_odometry_gray/dataset/sequences/*
!data/data_odometry_gray/dataset/sequences/00/